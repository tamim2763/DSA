#include<bits/stdc++.h>
using namespace std;

int main () {
    
    ios_base::sync_with_stdio(false);cin.tie(NULL);


    // see the reason from the one's complement of 0 image from the folder competitive programming or see the notes from the notebook
    cout << ~(0) << endl;

    cout << ~(13) << endl;

    
    return 0;
}